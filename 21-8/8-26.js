var N826;
(function (N826) {
    any[]
        ? T[number]
        : T;
    object
        ? T[K]
        : never;
})(N826 || (N826 = {}));
[T, readonly, any[]
        ? number : , keyof, T
        ? number
        : keyof, T,
    keyof, T];
object ? ReferenceValue(T) : ;
const size = ["large", "normal", "mini", "small"];
const ;
any ? (v) => any : never;
(v = R) => any
    ? R
    : never;
any[]
    ? T : ;
[infer, R, infer, I, ...any]
    ? [R, I]
    : never;
never;
object ? 1 : 2;
//# sourceMappingURL=8-26.js.map